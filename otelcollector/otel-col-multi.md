# Sketches for opentelemetry-collector

Uses cobra.Command to execute each Pipeline.

The plan is to create a Collector, 
which creates a separate pipeline for each YAML configuration

Multiple Config files?

One cobra.Command
